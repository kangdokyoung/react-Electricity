import {
    atom,
    atomFamily,
    selector,
    selectorFamily,
  } from 'recoil';

  
export const modalTemperature = atom({
    key : 'main/modalTemp',
    default : false,
  });

export const modalweekend = atom({
    key : 'main/modalTemp',
    default : false,
  });

export const modalvariable = atom({
    key : 'main/modalTemp',
    default : false,
  });